import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    wrapper: {
        width: '100%',
        height: '100%',
        display: 'flex',
        flex: 1,
        justifyContent: 'center',
        padding: 0,
        backgroundColor: '#fff'
    },
});
